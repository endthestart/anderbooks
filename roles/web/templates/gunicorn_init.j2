description "{{ application_name }}"

start on (filesystem)
stop on runlevel [016]

respawn
setuid {{ gunicorn_user }}
setgid {{ gunicorn_group }}
chdir {{ application_path }}

exec {{ virtualenv_path }}/bin/gunicorn_start
